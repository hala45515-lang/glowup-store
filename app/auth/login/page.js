import LoginClient from "@/components/auth/LoginClient";

export const metadata = {
  title: "Sign In — GlowCart",
  description: "Sign in to your GlowCart account.",
};

export default function LoginPage() {
  return <LoginClient />;
}
