import SignupClient from "@/components/auth/SignupClient";

export const metadata = {
  title: "Create Account — GlowCart",
  description: "Create your GlowCart account.",
};

export default function SignupPage() {
  return <SignupClient />;
}
